import { Routine } from "../../lib/dateMethods";

export default function Daily({ routine }: { routine: Routine }) {
    return (
        <div className="daily">
            <h1>Daily</h1>
        </div>
    )
}